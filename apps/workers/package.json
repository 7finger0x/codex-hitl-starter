{
  "name": "@ai-trading-os/workers",
  "version": "0.0.0",
  "private": true,
  "description": "Asynchronous outbox, export, evidence, and reconciliation runtime boundary.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": "24.18.0",
    "pnpm": "11.11.0"
  },
  "scripts": {
    "dev": "node --import tsx src/main.ts",
    "test": "node --import tsx --test src/worker.test.ts",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  }
}
